# ── Secrets — PREENCHA ANTES DE APLICAR ──────────────────────────────────────
#
# NUNCA faça commit deste arquivo com valores reais.
# Opções recomendadas para gerenciar segredos em produção:
#   - OpenShift Secrets Store CSI Driver (Vault, AWS Secrets Manager)
#   - Bitwarden Secrets Manager Operator
#   - Sealed Secrets (kubeseal)
#   - oc create secret generic bugbounty-secrets --from-env-file=.env
#
# Gerar JWT_SECRET seguro:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
#
# Gerar MONGO_PASSWORD seguro:
#   openssl rand -base64 24

apiVersion: v1
kind: Secret
metadata:
  name: bugbounty-secrets
  namespace: bugbounty
type: Opaque
stringData:
  MONGO_PASSWORD: "changeme"
  JWT_SECRET: "0421b7311ef10a27b8c4285eeaf81bd29db739a2500bf3a48f602333bc9a2911"
  ANTHROPIC_API_KEY: "CHANGEME_sk-ant-your-key-here"
  HACKERONE_API_USERNAME: "jackpach2084"
  HACKERONE_API_TOKEN: "FfSaYwK4vQdoQFg/6bWVgK8GUe+MHuL3Y2mZkF3p5s4="
