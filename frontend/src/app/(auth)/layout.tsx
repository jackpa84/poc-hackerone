/**
 * Layout das páginas de autenticação (login/register)
 * Não usa a Sidebar — layout limpo e centralizado.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
